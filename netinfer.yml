name: netinfer
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python>=3.8
  - pip
  - snakemake>=9.0.0
  - pandas
  - numpy
  - pyyaml
  - pip:
    - "."  # Install the netinfer package